const initialState = {
  loading: false,
  error: undefined,
  objects: undefined
}

export default function hashtags(state=initialState, action={}) {
	//console.log(action.type,state)
  switch (action.type) {
  default:
    return state
  }
}