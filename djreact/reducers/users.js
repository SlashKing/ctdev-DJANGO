const initialState = {
  loading: false,
  error: undefined,
  objects: undefined
}

export default function users(state=initialState, action={}) {
  switch (action.type) {
  default:
    return state
  }
}